language: node_js
node_js:
  - "node"

script:
  - echo "Gulping"
  - gulp sass

install:
  - npm install -g firebase-tools
  - npm install -g gulp

after_success:
  - firebase deploy --token $FIREBASE_TOKEN